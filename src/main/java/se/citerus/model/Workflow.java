package se.citerus.model;

record Workflow(
        String badge_url,
        String created_at,
        String html_url,
        Integer id,
        String name,
        String node_id,
        String path,
        String state,
        String updated_at,
        String url
) {
}
